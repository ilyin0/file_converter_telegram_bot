name: file_converter_telegram_bot
description: A server app using the shelf package and Docker.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

dependencies:
  teledart: 0.6.1

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
